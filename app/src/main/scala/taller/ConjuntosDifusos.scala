package taller

class ConjuntosDifusos {
  def grande(d: Int, e: Int): ConjuntosDifusos = {
    require(d >= 1 && e > 1)
    val cantInicial = 1
    val cantFinal = 10000

    @annotation.tailrec
    def inicializador(): ConjuntosDifusos = {}
  }

}
